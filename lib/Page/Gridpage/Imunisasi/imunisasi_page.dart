import 'package:flutter/material.dart';

class FormImunisasiPage extends StatelessWidget {
  const FormImunisasiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Imunisasi'),
      ),
      body: const Center(
        child: Text('Ini adalah halaman Imunisasi'),
      ),
    );
  }
}
